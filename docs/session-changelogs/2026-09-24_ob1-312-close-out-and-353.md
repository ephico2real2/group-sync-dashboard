# Session change log — group-sync-dashboard, 2026-09-24 → 2026-09-25

What this session did, when, and how each claim was measured. Times are git author times in America/Chicago; a PR's
merge time is its merge commit's on main. Every "measured" claim is one the session ran a command for; nothing below
is recalled from memory alone. The session is OB1's, taken over from the orchestrator at 20:16 on 2026-09-24 with two
tasks: close #312 on the lab, and specify — then build — #353. The product changelog (`docs/CHANGELOG.md`) says what
each release changed for an operator; this says what a working session did.

Outcome in one line: **to be written at the session's end.**

| | Before the session | After |
|---|---|---|
| main | `244d4ab` (#355 merged, 2026-09-24 20:15) | |
| chart / app | 0.53.1 / 0.32.0 (both under Unreleased) | |
| deployed on the lab | `4f4c070b08` through Argo CD; #354 merged, not deployed | |
| open PRs | none | |
| issues | #312 open (fixed in #350 and #354, not closed), #353 open (no spec) | |

---

## Part 1 — #312's close-out on the lab (2026-09-24)

### #312 closed out on the lab (20:18 → 20:57) — commit `1c5a81b`, PR #356

- `local-development/release-crc.sh --argocd` from a detached worktree at `244d4ab`: both images built and
  stamp-verified as `0.32.0-244d4ab9c0`, the Application Synced/Healthy at 01:20:51Z, `244d4ab9c0` verified in-pod;
  the two PVCs' UIDs identical before and after (`f065b7a4-…`, `08c7d45c-…`).
- The previous folder's walk scripts re-run as copies against the new pod: the auditor binding labelled, `can-i`
  yes / no, 0 unmanaged on every cluster, the tiles 205 = 38 + 6 + 161; a planted hand-made grant 205 → 207
  (unmanaged 1, Overview 7, KPI 21) → 205, and every cluster back at 0 once `shared-rnd`'s own refresh had run.
  Committed as `reports/2026-09-24_312-close-out/`.

### Review pass 1 on `1c5a81b`, its findings applied (20:57 → 21:12) — commit `803bf79`, PR #356

- **Found by Grok, Codex and OB1-lite** (review of #356, all three seats): every pod time the README labelled CDT
  carries `-0400` in the `.out` files, the pod's `TZ` is `America/New_York` and the pictures print EDT. **Accepted**
  in OB1-lite's form (label the pod's zone, add one sentence on the three clocks); Codex's conversion of the times to
  CDT **rejected**, because the same folder's screenshots print EDT and a reader comparing a log line to a picture
  would see two clocks.
- **Found by OB1-lite** (Codex noted the numbers were in no file): the 44 labelled Group-subject bindings include the
  chart's own auditor binding, so the policy operator accounts for 43, not 44; and 51 / 44 / 8 were measured but
  quoted from no file in the folder. **Accepted:** `walk/config-source.sh` and its `.out` are committed (51 labelled,
  44 with a Group subject, `group-sync-dashboard` 8 of which 1), and the README and the index row say 43. Also: "the
  poller logged it at 21:36:49" was the script's `tail -1` (the first sighting was 21:26:49, seven lines in all) and
  "shared-rnd's own refresh came at 21:47:50" was the last refresh before the re-read, not the first after the
  deletion (21:42:49). **Accepted:** `walk/pod-log-excerpt.sh` and its `.out` capture the pod's refresh and finding
  lines, and the README quotes them. And `347-kpi.png` is clipped at 700 px and shows no To review value.
  **Accepted:** the table row says the 18 is quoted from `verify-fixes.out`.
- **Found by Codex:** the "refreshed 207" line and the twelve-minute budget were inference, quoted from no file.
  **Accepted on the fact; Codex's softened wording rejected** — the pod-log excerpt turns the sentence into a quote,
  and the budget is attributed to `evidence-347.sh`. Codex's replacement for the #354 paragraph, which dropped the
  label inventory, **rejected**: the measurement is committed instead.
- **Found by Grok and OB1-lite:** the README cited "SPEC_U1, in its own pull request" before that PR existed, and
  "re-pointed at this folder" misdescribed the scripts, which point at the session's scratch folder. **Accepted:** the
  DoD paragraph cites #353 and, now that it exists, PR #357; the provenance sentence says where the scripts point.
  Grok's citation-ambiguity point on `walk/verify-fixes.sh` **rejected**: `tests/test_docs_citations.py` passes on the
  head (1037 passed, measured by Codex, OB1-lite and the author; Grok's seat has no shell).
- **Found by OB1-lite:** this log's Part 1 was written before its commit had passed review, against the changelog
  skill's rule, and its heading carried no commit or PR. **Accepted:** Part 1 is rewritten in that form. The second
  pass then found the rewrite itself wrong — it put this commit's content under `1c5a81b`'s heading, with an end time
  that matched no commit — so the entry is split as it stands here, each commit under its own heading, and the second
  pass's own findings are logged under the commit that applied them, once that commit has passed.

### Review pass 2 on `803bf79`, its findings applied, #356 merged (21:12 → 21:36) — commits `ae1135b`, `a450289`, PR #356

- **Found by Grok, Codex and OB1-lite:** the Times paragraph cited `347-access-granted.png` for "updated 21:24:49
  EDT"; that picture prints 21:24:47 (viewed) and 21:24:49 is `347-kpi.png`'s. **Accepted**, my error retracted.
  The attribution sentence still over-claimed (the 700-px clip is `verify-fixes.sh`'s, the page time a picture's, the
  Argo stamp the `.log`'s, the PVC stamps the `.txt` files', the chart version the tree's). **Accepted**: it names each
  source.
- **Found by Codex:** the intro said the planted-grant script "reads the refresh count it waits for"; it reads the
  baseline and waits for baseline + 1, which the 207 refresh overshot. **Accepted.** Five bare pod times could each
  carry `−0400`. **Rejected**: the Times paragraph fixes the zone once. A note on the scripts' scratch `lib.sh`.
  **Rejected** on the previous folder's precedent.
- **Found by OB1-lite:** "every count is the previous folder's" had one exception (`shared-rnd`'s first read after
  the removal, 1 vs 0). **Accepted.** "One second before Argo CD reported Synced/Healthy" was the waiter's 15 s poll.
  **Accepted** in its wording. The session log put `803bf79`'s content under `1c5a81b`'s heading with an end time
  (21:02) matching no commit (git: 20:57 / 21:12), said "all three seats" ran a test Grok's seat cannot, and claimed
  an order the timestamps refute. **Accepted**: split per commit with git's times, as it now stands above.
- PR #356 merged as `a450289` (`merge-safe.sh`, checks 8/8 green on `ae1135b`); #312 closed with the DoD comment
  pinned to that sha, every raw URL answering 200.

## Part 2 — SPEC_U1, the specification for #353 (2026-09-24)

### The spec written (20:18 → 20:56) — commit `d898b7b`, PR #357

- Research first: the three subject kinds, the `apiGroup` defaults and the ServiceAccount namespace rule read from
  upstream (types.go 34–36, validation.go 229, rule.go 290–300, kubernetes/kubernetes `90777f0b`); the lab measured
  (897 bindings; 205 / 685 / 35 subject entries; 26 namespace-less ServiceAccounts on 13 RoleBindings; 51 labelled,
  the 7 labelled ServiceAccount rows all the chart's; 0 exceptions); every reader of the binding table mapped.
- The first scratch application of my own blocks refuted three of them before any reviewer saw the spec — the
  `_OBSERVATION_SEEDS` narrowing (migration 14 splices those statements: "no such column" on a v0 database), the
  audit-stamp fixture's fake rows, and a test that contradicted invariant I3 — each recorded in the spec's notes.

### Review pass 1 on `d898b7b`, its findings applied (20:56 → 21:47) — commits `7bf6e1b`, `25b6d56`, PR #357

- **Found by Codex (F1) and Grok (C16):** my single gate over every subject kind silenced every unlabelled account
  and person by default on a host with no label anywhere — a default that silences, which the operator's rule
  forbids. **Accepted, my design retracted**: no gate for ServiceAccount and User subjects; the Group arm keeps
  #354's gate byte for byte, read over Group rows only; the asymmetry is stated for the operator on #353.
- **Found by Codex (F2):** `policyPage` returned a "not installed" card before its hero when the operator was
  absent, hiding every finding. **Accepted on the fact; snippet rejected** (it dropped the message, and
  `configsCard` renders nothing for an absent operator): the card renders beside the findings, with a Playwright
  test in `TestRbacPolicyPage`; Codex's Node harness **rejected** (the suite drives the real page).
- **Found by Grok (F1):** migration 20 was said not to be crash-safe (a populated `_v20` table left by a crash).
  **Accepted on the property, premise refuted by measurement** (Codex C5 and OB1-lite C5 measured the same): with the
  store's own connection the copy, the drop and the version write ride one implicit transaction (a simulated crash
  left 0 rows in the new table and version 19); `OR IGNORE` is added for the hand-repair case, with a test that
  builds that state.
- **Found by Grok (F2), Codex (F3) and OB1-lite (C11):** the compliance snapshot's "Group bindings" (Group rows) sat
  beside "Unmanaged" (every kind), and "Platform identity grants (excluded)" is excluded only from the direct-user
  figures. **Accepted**: the three labels name their populations and the every-kind total is printed (OB1-lite);
  Codex's explanatory note **rejected** as the larger change.
- **Found by Codex (F4):** the S4c version moves should be blocks. **Rejected**: the implementation PR applies onto
  main after this spec PR merges, when S4c's cell already reads 0.34.0 — such a block could not apply.
- **Found by OB1-lite (five):** a subject an OLM RoleBinding names twice was two rows in the reader and one in the
  store (10 on the lab: 925 entries, 915 rows — re-measured on the lab JSON), so every count in the spec moves to the
  stored one (703 finding rows on 689 bindings); "all OLM-written" was wrong (9 OLM, 3 cluster-version operator, 1
  hand); the findings page ordered by name across tiers dropped a dangling group behind the accounts; a schema-19
  snapshot raised "no such column" during the roll; the report form's role picker offered roles bound only to
  accounts (356 vs 66). **All five accepted**, with OB1-lite's tests.
- **Asked (C10), all three seats:** a User cell drilling to the user page. **Rejected** by all three seats and me:
  `system:kube-scheduler` has no User object; a span, like a virtual group.
- **Asked (C6):** `_discovered` reading every kind. Grok and Codex said right; OB1-lite showed its consumer (the
  privileged-access `roles`) reads Group rows and `user_binding` only — **OB1-lite's measurement wins** (the role
  picker above).
- Measured on a scratch copy of main with the revised spec's 104 blocks applied: `test_unmanaged_subjects.py` 32
  passed; the hermetic suite 5014 passed, 16 skipped; the three browser classes 47 passed. The decisions are on #357.

---

## Part 3 — the platform rule: SPEC_U1 amended, #360 re-derived, deployed, proved (2026-09-24 → 2026-09-25)

### The mandate, the amendment written (22:30 → 23:47) — commit `bf4defc`, PR #361

- The orchestrator's mandate file for #353 (the operator's rulings of 2026-09-24) superseded every earlier brief: a
  ServiceAccount subject is the platform's iff `settings.platform_namespaces.matches(<effective namespace>)`, a User iff
  `is_platform_user`, OpenShift's two per-project controller bindings are shipped defaults on an exact three-part match
  (the third is the store's Group `system:` arm), SA `default` has no name rule, nothing else excludes; a marker
  `PLATFORM-CLASSIFICATION (#255, #353)` on every deciding or consuming line, held by a guard test. The operator's later
  rules, binding on every seat: no hallucination, keep the code simple (reuse the classifier; one constant, one
  exact-match check; no framework).
- Written as an amendment to SPEC_U1 with its blocks, the orchestrator's notes (the rulings quoted, the "no
  inference" brief error and its retraction, the SA census), §3.12 (the marked sites) and the measurements. A second
  constant for the Group binding that nothing read was removed under the keep-it-simple rule.
- Measured with `measure-353.py` on the 2026-09-24 lab dump, `gsd` imported from the applied tree: 703 unmanaged
  SA/User rows → **124 rows on 117 bindings** by the defaults (86 of the silenced rows the two controller defaults
  alone) → **49 on 46** with the values reference answer.
- The logic review the mandate asked for (values.yaml → template → loader → classifier → poller → store → finding →
  pages, API, metrics, reports; per hop what it does, its test, any defect) posted on #360 with the marker listing. No
  defect in the existing or the new logic; one test gap closed (the loader on an empty stanza and the suffix axis);
  one observation left for the orchestrator (the chart drops `names: []`, a hand-written file does not — #255, safe
  direction, ruled out of scope).
- Proof on main `35fcddb` + the blocks: hermetic 5051 passed / 16 skipped (CI's command), browser 605, mock-cluster 59
  (an isolated venv with CI's install), helm lint clean.

### The owner's review and the four seats (23:47 → 00:37) — commits `9c80a19`, `7889959`, `f060e82`, `97dc9be`, `afe2eb5`, PR #361

- **The orchestrator (owner):** the `_FINDING_CASE` provenance comment still said nothing about a `system:` name or a
  platform namespace excludes a SA/User row, contradicting the arm above it. **Accepted** (`9c80a19`), with twelve
  other survivors of the retracted wording found by grep (the title and index row, the notes, §3.1, §3.4, two page
  blurbs, the chart's README and Chart.yaml paragraphs, the audit design document, the reference architecture, API.md,
  the CHANGELOG entry, three test docstrings). Its second finding, §4's "`system:kube-scheduler` is `unmanaged` and
  never `built_in`": **accepted** — the store reads the flag, not the name.
- **Grok** (read-only, its shell refused): the guard misses a bound-method pass, Home's live shape — **accepted** as
  one regex widening with a self-test; §3.12's count and the unnamed default site — **accepted**; a weak
  `assert x or len(got) == 1` — **accepted**, dropped; alias/`getattr` evasion — **rejected**, a regex's limit.
- **Codex Astra** (measured, E1–E10): F1, the "another role" negative was overwritten before its assert (the key has
  no role columns; `INSERT OR REPLACE` folded two shapes into one row; its mutant proved the old test blind) —
  **accepted**, one refresh per shape; F2, real consumers unmarked (`home.py` `ns_row`, `state.py`'s alert, the
  Poller's hand-off and refresh-line count, Home's cluster-wide counts, five store predicates, six page sites) —
  **accepted**, every site marked and `state.py` on the marked path; F3, contradictory prose — **accepted**; F4, two
  API.md blocks nested a fence in a fence so the applied file carried a literal ```` ```markdown ```` line and lost the
  field explanation and the `oc label` alternative — **accepted**, five flat blocks; C7 "byte for byte" — **accepted**,
  reworded.
- **OB1-lite** (measured, 5656 passed): N1 the same prose survivors plus the OpenAPI description, Chart.yaml, the
  CHANGELOG body, §5.4's "backlog of 203" — **accepted**; F1 the guard should hold the stored flag's SQL comparisons
  and Python assignments — **accepted**, one alternation; N2 the chart's own 7 rows move `ok` → `built_in` under the
  values answer — **accepted** in §5.7; Q1 a ServiceAccount spelt as a User is silenced by the `system:` prefix and
  bypasses the SA-`default` rule — a **question**; Q2 migration 20 edited in place against a database that ran #360 —
  a **question**; Q3 an AST guard — **rejected**.
- **OB2** (measured): C3's constructed misses and the `derive_answer` callable default — **accepted**, one marker;
  the brief's `PYTHONPATH` corrupted by a placeholder substitution — **accepted**, the re-run brief fixed.
- **The operator's rulings** (`f060e82`): Q1 — keep it silent, no namespace rule, recorded with the measurement (5
  such rows on the lab, all `openshift-kube-apiserver`) and the consequence; Q2 — read `PRAGMA user_version` from the
  pod before the deploy, 19 deploys as is, 20 without the column stops.
- **Second pass on `7889959`** (both seats re-run with the corrected brief): OB1-lite MERGE — N1, no test exercised
  the Poller's hand-off of `settings.platform_namespaces` (with the keyword dropped the suite stayed green) —
  **accepted** as a test only (`97dc9be`; re-measured: passes on the tree, fails on the mutant); N2/N3 wording
  (what the guard holds; "703 since #353" → "703 before the rule, 124 after") — **accepted**. OB2 MERGE — F1, the
  §3.12 sentence over-stated the guard ("every read or write") — **accepted** as wording (`afe2eb5`); Fix B and
  OB1-lite's Q1 alternations — **declined by the orchestrator, the regex's stated limit**.
- CI green on all 8 checks at every head; `merge-safe.sh 361` → main `a79da71` (00:44).

### #360 re-derived from the merged spec, four seats, merged (00:45 → 01:04) — commit `0199e21`, merge `c00aa2e`, PR #360

- The branch's tree made main's (`a79da71`), the 190 blocks applied with `apply-spec-blocks.py`, the two status rows
  moved to `in progress`; a fresh apply of the blocks onto main reproduces the tree up to those rows (`diff -r`,
  measured by me and by three seats). Full suite on the tree: hermetic 5062 passed / 16 skipped, browser 605,
  mock-cluster 59, helm lint clean; CI green on all checks including `mock-cluster`.
- Grok MERGE; Codex Astra MERGE (empty patch; its two hermetic errors were its sandbox refusing a loopback bind in
  `test_fleet_login.py`, untouched); OB1-lite MERGE (nine mutants of the rule each killed); OB2 MERGE (six in-place
  mutants; a migration harness: a populated v19 upgrades with 4 of 4 rows carried, a version-20 database without the
  column fails on `is_platform` in the store and in the report service — the pre-deploy read is the one guard for
  both). The one refutation across the seats: my brief's "fails when you remove any marker" — the guard holds 36 of
  61 by its documented scope (measured by OB1-lite, Codex and OB2 alike); the other 25 are by hand. Two wording items
  for the close-out (§3.12's "six" snapshot predicates are seven; a `PYTHONPATH` note beside the measurement). The
  orchestrator signed off; `merge-safe.sh 360` → `c00aa2e`.

### Deployed and proved on the lab (01:04 → 01:41) — `c00aa2e`, then `de303e9` (PR #362 at 01:16)

- Pre-deploy reads: PVC UIDs `f065b7a4-…` and `08c7d45c-…`; Argo Synced/Healthy at `244d4ab9c0`; the pod's
  `/data/gsd.db` at `user_version 19` without `is_platform` — deploy as is.
- `release-crc.sh --argocd` from main `c00aa2e`: `0.33.0-c00aa2e4ff` built, Synced/Healthy 06:06:09Z, verified
  in-pod, PVC UIDs identical; migration 20 applied on start; the first refresh stored 919 rows (206 Group, 678 SA,
  35 User). Under the shipped defaults: **125 unmanaged rows on 118 bindings** (114 SA, 11 User), 743 built-in (581
  by the flag), the tiles 919 = 45 + 131 + 743. Against the spec's 124/117: one row more, named —
  `RoleBinding envoy-gateway-system/system:openshift:scc:nonroot-v2` → SA `envoy-gwapi-demo-eg-8fbae7fe`, an SCC
  binding OpenShift wrote at 02:17Z on 2026-09-25 for the `gwapi-demo` Envoy proxy — the lab drift the mandate leaves
  alone (`walk/drift-vs-dump.txt`, row by row).
- #362 (`environments/crc.yaml` gains the reference `platformNamespaces` block; its README row for the parity test)
  merged after the 125 was recorded; redeployed: Synced/Healthy 06:18:01Z, PVC UIDs identical, the ConfigMap carries
  the block. **50 rows on 47 bindings** (39 SA, 11 User; the seven named namespaces gone from the list), `ok` 45 → 38
  (the chart's own 7 rows to built-in, as §5.7 predicts), built-in 825; the same one drift row against the spec's
  49/46.
- The planted grant: `plant.sh` created the namespace, account and ClusterRoleBinding at 06:18:44Z and failed on its
  check line (an f-string with escaped quotes in its braces — the orchestrator caught the blind wait); `plant2.sh`
  continued without re-planting: `unmanaged` at 06:23:47Z with the WARNING naming
  `ServiceAccount gsd-evidence-353/u1-evidence-sa`; labelled → `ok` at 06:29:53Z; a grant to a new account in
  `openshift-monitoring` → `built_in 1` at 06:34:48Z, 0 log lines; both bindings deleted by name (cluster-scoped), the
  account and the namespace removed, both rows absent at 06:40:53Z. The lab back at 50 / 47; `shared-qa` and
  `shared-rnd` (the same cluster, by design) read the same; the lab handed back free.
- Evidence: `reports/2026-09-25_353-platform-rule/` (README, `walk/`, `screenshots/`) with its `reports/README.md` row.
