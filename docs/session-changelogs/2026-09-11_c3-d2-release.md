# Session change log — group-sync-dashboard, 2026-09-11 → 2026-09-14

What this session did, in order, with the commit, PR or measurement behind each line. Times are the
git author times on this machine (America/Chicago). Every "measured" claim is one the session ran a
command for; nothing below is recalled from memory alone.

Outcome in one line: **C3 (reporting service) and D2 (per-cluster authorization) released; the
thirteen-module programme is complete, milestones R1–R7 closed.**

| | Before the session | After |
|---|---|---|
| Application / chart | 0.17.0 / 0.19.0 on main | **0.19.0 / 0.21.0** on main, published and verified |
| Images on quay.io | one repository | two, both signed, SBOM-attested, SLSA-attested per release |
| Programme | 11 of 13 modules released; C3 mid-review with 13 uncommitted files | 13 of 13; R6 and R7 closed |
| Open PRs / issues | #91 open; #67, #68 open | none; #67, #68 closed with release notes |

---

## Part 1 — C3, reporting as a microservice (2026-09-11 evening, 2026-09-13 afternoon)

### Resume and the publish-job defect (09-11, 18:09 → 19:06) — commit `18438c5`

- Read the resume note and `docs/REVIEW_C3.md` in full before touching code; lifted the pause note in memory.
- **Fixed the report-image publish step** — it had landed under the `sbom` job while reading step outputs
  that exist only in `publish`, so CI could never have pushed the report image (Codex's pass-1 finding).
  Moved into `publish` after the dashboard's build, with its own digest file and a second pair of job
  outputs; `sbom` and `attest` became a two-leg matrix over both images (`DESIGN_supply_chain.md` D10).
- Extended the workflow-semantics test to hold every `steps.`, `needs.<job>.outputs.` and `matrix.`
  reference to a definition. Verified the change before commit by three independent passes (GitHub's
  documented context availability, ten failure scenarios, eleven reintroduced defects — every mutation
  killed by the test naming it) and actionlint with shellcheck.
- From that verification: the manual release route named one script where a release is now two images
  (fixed in the release decision's warning, `helm.yaml`, the script header, `RELEASING.md`).
- **Found by the full suite:** three report-server tests built their app on the real clock while the
  module mints tickets at import, so a suite reaching the module after the 300 s TTL answered 401.
  Reproduced by ageing the mint time; fixed by giving every app the frozen clock.
- Measured that `quay.io/ephico2real/group-sync-dashboard-report` did not exist; recorded in
  `RELEASING.md` that a new image's repository must be created public by hand (Quay creates on push
  as private, the chart pulls anonymously). The operator created it during the session.
- Full hermetic suite 2738 passed. Pushed; PR comment with every decision.

### Second pass, Cursor half (09-11, 19:31) — commit `fc11e75`

- Accepted, each measured first: an integer parameter string must spell digits only (`int()` took
  `" 1"` and `"1_000"`); a `str` parameter must be a string; PDF cells collapse whitespace before the
  cap (600 newlines was still a page-tall row); a 200 whose JSON is not the usage feed counts `error`.
- Rejected: adding `needs.publish.result == 'success'` to the `sbom` job — GitHub applies `success()`
  by default when a condition names no status function.
- **Found by CI, not a reviewer:** the `image` job went red because the `hi/python:3.14` base had
  been rebuilt on 2026-09-09 and its rpm now leaves `.keyring.lock`; the build-time proof compared
  an exact two-file listing. Both proofs now hold a rule ("rpmdb.sqlite plus rpm's lock files"), and
  `DESIGN_hardened_image.md` records it. Full suite 2744 passed.

### Second pass, Codex half (09-11, 19:48) — commit `cc98c82`

- **The pass's most severe finding, found by reading alone:** the report wrapper defaulted `IMAGE_NAME`
  from the environment, so a shell that had exported the dashboard's `.env` would have tagged report
  bytes under the dashboard's name and, with `--release-tags`, moved the dashboard's aliases onto
  them. The wrapper now forces the name; a fake-tool test runs the real release path with the
  environment poisoned.
- The PDF cell cap is 240, measured against the catalogue's widest table (`W`×600 failed the real
  six-column table while passing the two-column test helper); a truncated usage page that does not
  advance is `error`; the install guide qualifies "every image is signed" with the two switches.
- Rejected: a 422 for explicit `null`; requiring `service_version` in the feed; an ellipsis on
  whitespace-only normalisation; a publish preflight requiring the report repository to be public.
- Full suite 2759 passed. Codex's first launch had been cut short mid-review; relaunched on a
  pristine export of the commit.

### Live run and release (09-13, 18:08 → 18:35) — commits `4cacb81`, `31c0b36`, `e1dc930`

- Operator lifted the CRC pause; started CRC and the podman machine; logged in with a separate
  kubeadmin kubeconfig (the default `oc` context on this machine is a different cluster).
- Chart lint and every switch state per the spec's §12; both images built locally with their proofs
  (the pack stage's RPM directory showing the three names the new rule allows).
- Deployed both images to CRC (`0.18.0-cc98c820ac`), ran the live check: readiness in 26 ms over TLS,
  a service-principal run to a 3-page PDF/A-2b document with every marker, the second pass's fixes
  measured live (a wrong shape and a padded integer → 422, a spliced ticket → 403), the usage pull
  `ok`. Recorded in `REVIEW_C3.md` with the outcome paragraph.
- **Merged PR #91** (CI green on 4cacb81) → `31c0b36`; #67 closed. Docs PR **#92** merged → `e1dc930`.
- Verified the release from the laptop: both images' `0.18.0-31c0b36323`, `0.18.0` and `0.20.0` tags
  resolve to one digest each; `cosign verify`, the SBOM attestations (85 and 91 packages, Syft 1.51.1)
  and `gh attestation verify` on both; chart 0.20.0 in the index with its attestation, rendering both
  images at 0.18.0. This was the first real run of the two-image supply chain: five jobs green.
- Release note on #67; milestone R6 closed.

---

## Part 2 — D2, per-cluster authorization (2026-09-13 evening → 2026-09-14 02:48)

### Applied from the spec (09-13, 19:11) — commit `05cbf3f`, PR #93 opened

- Loaded the project skills into the session on the operator's instruction and recorded it as a
  standing rule in memory.
- Bumped to app 0.19.0 / chart 0.21.0 with `prepare-release.py`.
- Extracted the spec's 43 fenced blocks mechanically and applied them with an exact-match check on
  every Old text — all 47 edits matched a tree seven releases newer than the design.
- Deviations found while applying, all recorded in the spec's notes: the spec's `list_alerts` block
  predated B4 and had dropped the cliff policy (a test found it); four existing tests needed the
  wire's new shape; the NOTES test renders through the probe because `helm template` drops NOTES.
- Applied the two items earlier reviews had routed to D2: a ServiceAccount's `system:serviceaccounts`
  groups derived from the identity's shape (a namespace-group grant now admits it in the application
  as at the proxy), and the page refetching a new viewer's own path when a session changes hands.
- Posted the spec's two open operator questions on #68 with the fail-closed defaults taken.
- Full suite 2799 passed; browser suite 268 passed (after installing the Chromium build the pinned
  Playwright expects).

### First live check and a chart defect (09-13, 19:29 → 19:40) — commits `daa39a9`, `116a4f0`

- Deployed to CRC and ran a two-cluster check with a second entry pointing at the same API.
- **Found by the check:** with `--set clusters[1].…` on a values file that does not define the list,
  Helm pads index 0 with `null` and the new guard died on a Go nil pointer. The guard now refuses a
  non-map entry by index and names the remedy (pass every entry); a chart test pins the padded case.
- The check then passed end to end: the policy logged once at startup; whoami with one decision per
  served cluster; person-scoped views on the remote → 403 with the documented sentence, health still
  served; alerts at the narrowest scope; `hidden` answering the same 404 as an unknown id and absent
  from every list; `remote-sar` without `same-as-host` refused at render.
- The one-cluster restore first failed on the chart's post-upgrade hook exceeding its deadline; re-run
  with the hook pod watched: unschedulable for 70 s on a node at 95 % CPU requests, then completed —
  lab capacity, not a code path.

### Review, first pass (09-13, 20:48) — commit `91c5554`

- Both reviewers on the required models, each to its own output file, Codex on a pristine export.
- **Accepted, each measured first:** `inherit` meant the host's *resolver*, so a `self-only` host
  with an `inherit` remote served the remote wide (Cursor; host `self`, east `all`, findings 200
  before the fix); a blank `visibility`/`identity` rendered through the chart's guard and crashed the
  pod at startup (Codex); the hand-built alerts had lost `silenced`/`silenced_by`; the pill tested
  `=== "self"` while the helpers test `!== "all"`; NOTES took index 0 as the host; two template
  comments landed inside the ConfigMap's config; `/api/events` in §11 does not exist; the Reports tab
  now follows the host's headline; `helm.yaml`'s label-step error claimed the chart "will still
  publish" after `exit 1`.
- **Rejected with reasons:** byte-identical 404 bodies for hidden and unknown ids (the id is the
  caller's own input); DNS-1123 validation of a ServiceAccount's name (a name no apiserver issues
  grants nothing); a 30-minute image wait loop in `helm.yaml` (the cross-workflow assumption the
  file forbids after #37); the prose-keyword doc tests.
- Eleven new tests. Full suite 2822 passed.

### Review, second pass (09-14, 01:24 → 02:35) — commits `b67a44b`, `3629c8c`, `c9a8c07`, `1652c09`

- Codex's first second-pass launch hung 4 h 16 min at "Reading additional input from stdin…": a
  backgrounded shell's stdin never closes. Killed, relaunched with stdin closed; the lesson is written
  into the review skill and memory.
- **Cursor's most important finding — a hole in my own pass-1 fix:** remapping `inherit` onto the
  host's policy then applied the remote's default `identity: none` and refused a self reader on such
  a remote, the opposite of what §11 says; my test had hidden it by setting `same-as-host`. Fixed:
  the cluster's own policy decides whether identity is consulted. Also: with every entry disabled
  the headline fell open to the host resolver (now fails closed); whoami and alerts decided the host
  twice per request (measured on `/metrics`; the headline is now the host row's decision); the
  selector still failed open on a silent scope; the Reports fetch guard is pinned by a test.
- **Codex's most important finding:** a quoted `enabled: "false"` in a values file is a non-empty
  string, truthy in Go and in `bool()`, so the chart guard, NOTES and the app all made the disabled
  entry the authorization host (measured). `enabled` is now a word in all three. Also: the Reports
  tab lagged a cycle behind a mid-session promotion (the Access granted tab's catch-up applied); the
  §5 decision diagram cited stale line numbers (anchors now, held to definitions by a test).
- **Rejected with reasons:** a test rendering `git archive main` at test time; a nine-site
  `narrowedScope()` refactor of the tab payloads' `=== "self"` (a closed server vocabulary); a policy
  badge on the Overview card. Codex's C1 CONFIRMED read the same cell Cursor refuted as fine — the
  sentence in §11 was the contract, and Cursor read it.
- **Found by CI:** the chart job runs without the application installed (C3's deviation 10 lesson,
  met again); the three-way test split across two modules.
- Deployed the reviewed head to CRC; the two-cluster check passed again, and the one-cluster restore
  went through cleanly. Recorded with an outcome paragraph. Full suite 2839 passed on a clean run.

### Release (09-14, 02:41 → 03:15)

- **Merged PR #93** (CI green on 1652c09) → `a9349a4`; #68 closed. Docs PR **#94** merged → `3965a40`.
- All three release runs green: publish (both images with `--release-tags`, two SBOM legs, two
  signing legs), Release Charts, CI.
- Verified from the laptop: `group-sync-dashboard` tags `0.19.0-a9349a46e8`/`0.19.0`/`0.21.0` → one
  digest (`sha256:ecdffd06…`), the report image's three → `sha256:9cad8f40…`, both stamped with the
  merge revision; `cosign verify`, SBOM attestations (85 and 91 packages) and SLSA provenance on both;
  chart 0.21.0 in the index (150,290 bytes) with its attestation, rendering both images at 0.19.0 and
  refusing `remote-sar` without `same-as-host`.
- Release note on #68 with the measurements and the upgrade guidance; milestone R7 closed.
- Local checkout returned to main; merged branches deleted; review exports removed; no reviewer or
  test processes left running.

---

## Numbers

| | |
|---|---|
| Commits authored | 16 on two branches, plus 4 merges |
| PRs merged | #91, #92, #93, #94 |
| Issues closed with release notes | #67, #68 |
| Review passes run | 4 (two per feature), each with both reviewers |
| Reviewer findings accepted / rejected | C3: 12 accepted, 8 rejected · D2: 19 accepted, 10 rejected |
| Defects found by tooling rather than reviewers | 5 (three test-clock 401s; the rebuilt base image; Helm's padded null; the chart CI job without the app; one flaky run whose name my filter dropped) |
| Full hermetic suite, final | 2839 passed, 14 skipped |
| Live runs on the reference cluster | C3 ×1 (both images), D2 ×2 (two clusters, hidden, refusal, restore) |
| Longest single loss | 4 h 16 min — a backgrounded `codex exec` waiting on stdin; now in the skill |

## Where things are recorded

- `docs/REVIEW_C3.md`, `docs/REVIEW_D2.md` — verdict tables, decisions with reasons, live-run
  measurements, outcomes.
- `docs/specs/SPEC_C3_reporting_microservice.md` notes 19–24 and `SPEC_D2_per_cluster_authorization.md`
  notes 1–10 — every deviation from the spec and every rejected snippet.
- `docs/DESIGN_supply_chain.md` D10 (two images, one chain); `docs/DESIGN_hardened_image.md` (the
  proof's rule); `docs/ACCESS_CONTROL.md` §11 (per-cluster authorization).
- The adversarial-review project skill (untracked, under the project's Claude directory) — the stdin launch rule.
- Memory notes: *c3-resume-point* (programme complete), *adversarial-review-before-shipping* (two new
  data points), *load-project-skills-before-autonomous-work*.

## State left behind

- CRC is running with one cluster on `0.19.0-3629c8ca27`. Stop it before the next kind test.
- Nothing is pending on the programme.
