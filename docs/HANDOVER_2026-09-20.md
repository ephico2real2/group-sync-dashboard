# Handover — 2026-09-20, end of the programme night session

Read this first in the next session. Every line below was measured at the time of writing (git, `gh`,
the lab); nothing is recalled. The memory index for this repository is at
`~/.claude/projects/-Users-olasumbo-gitRepos-group-sync-dashboard/memory/MEMORY.md` and carries the standing
rules (three-seat review, CRC deploy + walk, evidence on the issue, changelog, no attribution trailers).

## Where main stands

- `main` = `4f93ad1` — PR #228 (the Kyverno module, #165/#170) merged and **running on CRC**, walked on
  main (`reports/2026-09-20_kyverno-170/`, `errors : []`). Evidence with pictures is on #170.
- Programme items merged this session (in order): #224 (#143), #222, #225 (#153), #226 (#219), #227 (#170
  step 0), #228 (#170 module). All their branches and worktrees are deleted; the older merged branches
  from earlier sessions were left alone on purpose.
- Chart 0.41.0 published; `docs/CHANGELOG.md` Unreleased carries the Kyverno entry and (on PR #231) the Flux
  entry with chart 0.41.1.

## Open PRs

| PR | branch | state | what to do |
|---|---|---|---|
| #231 | `docs/212-flux-example` | open, unreviewed, CI pending | the Flux `HelmRelease` example (`examples/flux/helmrelease.yaml`, validated against the upstream CRD schemas; the chart README section; chart 0.41.1). Needs the three-seat review pass, then merge. Closes the last box on #212. |
| #223 | `docs/session-changelog-2026-09-19-programme` (worktree `~/gitRepos/gsd-changelog`) | open | the session changelog through Part 10 (`99a988e`). Parts 11 (#229) and 12 (#230 S1) were to be appended by the forks below. Merge when the session's work is logged. |

## Work that was IN FLIGHT in background forks when the session ended

The two forks below ran inside this session and **stopped with it**. Their worktrees and branches hold
whatever they had written; nothing they had not committed is lost from disk, but it is uncommitted. Read
each worktree's `git status` and `git log` before doing anything.

### #229 — the report library tab (worktree `~/gitRepos/gsd-library`, branch `feat/229-report-library`)

- Committed: `240e25d` the agreed mock `docs/design/report-library-mock.html` (operator: "I love the design";
  link words settled as **Generate this report →** / **Generate another →**); `fb1ce88` the spec
  `docs/specs/SPEC_E1_report_library.md`.
- Uncommitted at handover (9 files): `gsd/reporting/artifacts.py` + `server.py` (the `expires_at` /
  `retained_by` fields — the contract is in the spec: rank < keep is kept whatever its age, so `expires_at`
  is the EARLIEST it can go; `retained_by` `newest:<n>/<keep> of <schedule> on <cluster>` | `age:<days>d` |
  `manual:<days>d` | `manual:cap`; queued/running → null), `API.md`, `gsd/static/index.html` + `app.css`
  (the Library tab, `--tab-library #7a2468` light / `#d489c0` dark), `docs/CHANGELOG.md`,
  `docs/design/README.md`, the spec. The fork was waiting on its full suite when it last reported.
- To resume: `cd ~/gitRepos/gsd-library && git status && git diff --stat`; run the affected tests, then the
  full suite (from `local-development/`, the venv at `~/gitRepos/group-sync-dashboard/local-development/.venv`),
  commit against the spec, open the PR, deploy to CRC (`./local-development/release-crc.sh --argocd`,
  `KUBECONFIG=~/.kube/crc.kubeconfig`, clean tree), walk it (`reports/2026-09-20_report-library/`, model on
  the kyverno walk), the three-seat review, merge, evidence on #229. The plan was to be posted on #229 — check
  whether it was.

### #230 S1 — clusters as labelled Secrets (worktree `~/gitRepos/gsd-clusterconfig`, branch `feat/230-cluster-secrets`)

- Launched minutes before the handover; at the time of writing the worktree did **not yet exist**
  (`git worktree list` shows no `gsd-clusterconfig`). If it exists in the next session, read it; if not,
  nothing was started. The brief: research (Argo CD's cluster Secret + informer, the K8s watch semantics,
  Python config layering, ESO/Flux/Kustomize Secret shapes), then `docs/specs/SPEC_S1_cluster_secrets.md`,
  then the reader/contract/API (`GET /api/clusterconfigs`, administrator tier, credential KIND only), the
  chart Role on Secrets (get/list/watch), a migration after 18, tests, CRC validation with a real labelled
  Secret, three seats, merge. #230 carries the cited research and the proposal; #119's comment of
  2026-09-20 carries the measured as-is (tokens only; P1/P2 unbuilt).
- Coordination rule for both forks: only one `release-crc.sh` at a time on CRC (`pgrep -f release-crc.sh`
  empty and the Argo Application Synced/Healthy before deploying).

## Decisions taken this session (the operator's words, where they ruled)

- #229: the Reports and Reporting-status pages stay **exactly as they are**; the library is a new tab, a new
  canvas, one section per configured report, headed the way people say it ("the quarterly reports"). The
  heading word is the service's `cadence` ("Daily", "Quarterly", "1st & 16th"), never a regex on a name.
- #230: clusters as labelled Secrets, Argo CD's model — the label drives discovery, the Secret's name is a
  convention for people, `stringData.name` is the cluster id the dashboard shows; a Cluster Configurations
  tab writes the Secret; GitOps writes the same Secret. Supersedes #119 P1's mount mechanics; #119 P2 (the
  password-for-token exchange) stays needed and follows.
- Queue after the above: #119 P2, then #210 (Fable re-reviews when the quota resets), #160/#159 as before.

## Mocks — status

Every mock in `docs/design/` is implemented (#157, #158, #153/#167, #173, #149's status page) except
`report-library-mock.html` (agreed, #229 in flight). The design index's **Implemented** tags lag for five
of them — a docs fix. The one mock **not yet made** is the Cluster Configurations tab (#230 S2): build it
from the S1 API contract, from the lab's real `/api/clusters` payload (one cluster, `crc-local`; the mock
cluster from `environments/crc.yaml` is the second), render-check it (375/768/1280, both themes), publish
for the operator, commit on agreement.

## Lab

- CRC running; `main` (`4f93ad1`) deployed through the Argo Application `group-sync-dashboard` in
  `openshift-gitops`; kubeadmin password at `~/.crc/machines/crc/kubeadmin-password`; the report service's
  token Secret `group-sync-dashboard-report-shared-token` (query it over `https://127.0.0.1:8443` from the
  report pod with `Authorization: Bearer`).
- The mock at `docs/design/report-library-mock.html` was also published as a private claude.ai artifact
  for review; the committed file is the record.

## Waiters and processes

Every waiter/monitor this session armed is dead with it. Sweep stale processes on the next start
(`~/.claude/tools/sweep-stale.py` runs from the UserPromptSubmit hook).

## The resume prompt

> Read `docs/HANDOVER_2026-09-20.md` on main, then the memory index. Continue the programme: (1) finish
> #229 from `~/gitRepos/gsd-library` per its spec and the handover; (2) start or resume #230 S1 per the
> handover and #230's plan; (3) build the Cluster Configurations mock (#230 S2); (4) review and merge #231,
> then #223. Every PR: three-seat review, CRC deploy + walk, evidence on the issue, the session changelog.
> Never merge without CI green on the head's rollup; never deploy while another deploy runs.
