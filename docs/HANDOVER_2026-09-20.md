# Handover — 2026-09-20, end of the programme night session

Read this first in the next session. Every line below was measured at the time of writing (git, `gh`, the lab);
nothing is recalled. The lab lines of the 2026-09-24 update come from the D2b walk record and the orchestrator's
summary, not from a new read of the lab. The memory index for this repository is at
`~/.claude/projects/-Users-olasumbo-gitRepos-group-sync-dashboard/memory/MEMORY.md`. It carries the standing rules:
the three-seat review, a CRC deploy and walk, evidence on the issue, the changelog, and no attribution trailers.

## Living-document rule (operator, 2026-09-20)

This file is the state of record for the programme. It is kept current by a small docs PR whenever the state moves:
a merge, a decision, or a fork's start or end. The dated sections below are superseded by the newest "Update" block,
and git and `gh` outrank any line here.

**Update 2026-09-26, after #322 and #321 (main `e5459e0`).**

- **#322, the cluster-admin tier:** shipped in #372, chart 0.57.0 and app 0.35.0.
  - `visibility.clusterAdminSar` (`update clusterrolebindings`) gates KPI and Cluster Configurations, and grants
    every host tier.
  - Walked on the lab: `reports/2026-09-25_cluster-admin-tier-322/`.
- **#321, the OAuth Debug path removed:** shipped in #373, chart 0.58.0 and app 0.36.0.
  - Login capture is audit-log only. Values that name `authLogLevel.*`, `loginCapture.source: pod-log` or
    `loginCapture.namespace` fail the render.
  - Walked on the lab: `reports/2026-09-25_remove-oauth-debug-321/`.
- **The implementers are OB1-lite and Codex Astra** (the operator's mandate). OB2 no longer codes, and a change is
  never reviewed by the seat that wrote it.
- **Codex's sandbox forbids opening sockets**, so its browser and mock runs error. Run those suites outside it.
- **Closed:** #308, #321, #322, operator-chart issue 69. **Filed:** #371.
- The lab runs `667b3c4a62`. Session log Part 10 has the detail.

**Update 2026-09-25 morning, after #366, #367 and #368 (main `7aa00f1`).**

- **#367:** `charts/group-sync-dashboard/docs/UNMANAGED_GRANT_EXCLUSIONS.md` says how to silence a legitimate
  unmanaged grant (chart 0.55.1).
- **#368:** bindings refresh hourly by default (`config.bindingIntervalSeconds: 3600`). Cluster discovery and the
  #284 lookup's retry backoff keep 300s on their own setting, `config.discoveryIntervalSeconds`, which refuses values
  below 1. The liveness probe stays at 300s, by the operator's ruling. Chart 0.56.0, app 0.34.1.
- **Opened and parked:**
  - #369: fleet credentials as an existing Secret or an ExternalSecret, global or dedicated per stanza.
  - The operator chart's issue 70: deprecate its copy Jobs.
  - SharedSecret was ruled out (operator-chart issue 69, closed).
- **Closed:** #308, whose image job passed on 8 of 8 main runs. **Filed:** #371, the suite writes into the working tree.
- **Next, the operator's order:** #322 (the cluster-admin tier), then #321 (remove the OAuth Debug path). Session log
  Part 9 has the detail.

**Update 2026-09-25, after #353, #293 and #340 closed (main `35d2c0c`).**

- The session of 2026-09-23 → 25 merged 39 PRs, #309 to #365 (`gh pr list --state merged`, merged since 04:11 on
  2026-09-23). Its logs:
  - `docs/session-changelogs/2026-09-23_nsaudit-reports-and-remote-sar.md` (Parts 1–8);
  - OB1's `docs/session-changelogs/2026-09-24_ob1-312-close-out-and-353.md`.
- **#353 is closed** (OB1 on Fable: #357, #361, #360, #362, #364). The unmanaged finding covers ServiceAccount and User
  subjects:
  - platform identities are silenced by the configured classification (the namespace defaults plus `values.yaml`
    `platformNamespaces.additional*`, and `is_platform_user`) and by OpenShift's three per-project controller bindings;
  - anything else is silenced only by the operator's label or the exception annotation;
  - every site is marked `PLATFORM-CLASSIFICATION (#255, #353)`;
  - measured on the lab: 125 findings on the shipped defaults, 50 with the reference values.
- **#293 is closed** (#363, #365). Labelled ConfigMaps of cluster stanzas generate the `gsd-cluster-<name>` Secrets
  through the lookup:
  - Codex (gpt-6-astra) wrote SPEC_S5 and applied it; the orchestrator reviewed it with Grok and walked it on the lab;
  - `insecureSkipVerify` is allowed in a stanza, by the operator's ruling;
  - the chart adds only `configmaps get/list/watch`.
- **#340 is closed** (#358): a trusted CA bundle replaced in place is used without a restart. #359 completed the
  reports index (with a test), corrected S3's version and dated `docs/design/data-requirements.md`.
- **An incident:** OB1's end-of-task cleanup removed every git worktree. That included the operator's
  `~/gitRepos/gsd-grafana`, and its three uncommitted PNGs under `reports/2026-09-20_kyverno-170/` are
  unrecoverable: no local snapshot, no Time Machine. The committed versions at `4f93ad1` are intact. The rule is in
  memory (remove-worktrees-by-your-own-list).
- No PR is open. The lab runs `09d5526cae` under Argo CD. Its code equals main's; the later commits are reports and
  docs.

The sections below are rewritten to this state. The earlier updates of this file are in its git history.

## Where main stands

- `main` = `e5459e0`: #375, merged 2026-09-26 at 00:10 America/Chicago.
  - Chart 0.58.0 and application 0.36.0. `docs/CHANGELOG.md` carries every version since 0.24.0 / 0.33.0 under
    `## Unreleased`.
  - The annotated tag `checkpoint-2026-09-23` is on `7c0a42c`: main before D2b. Its message says "Not a chart
    release".
- **The session's merges:**
  - The audit-log capture default (#320) and the OB3 model pin (#323).
  - Three credential and polling documents: #317, #313 and #309.
  - SPEC_S4c (#325, issue #285), specified and not begun, now at application 0.37.0 and chart 0.59.0: U1 took
    0.33.0 / 0.54.0, S5 took 0.34.0 / 0.55.0, #368 took 0.56.0, #322 took 0.57.0 and #321 took 0.58.0, so S4c's reserved pair moved up (`docs/specs/README.md`).
  - The Namespace audit mock and #261 parts 2 and 3: #324 and #326–#330. #261 is closed, and part 4 was decided
    with no code.
  - The IME test race (#331), the Generate-bar focus fix (#333, closing #332), and the release walk and its record:
    #334 and #335.
  - The remote cluster access design and D3: #336 and #337.
  - D2b: #339, #342, #343, #344 and #345.
  - The session's docs (#349), #312's chart provenance (#350) and its follow-up (#354), the specs and design
    indexes (#351), and #346 and #347's page fixes (#352).
  - The evidence and handover (#355), #312's close-out (#356), the CA cache (#358) and the indexes (#359).
  - #353: SPEC_U1 (#357) and its amendment (#361), the code (#360), the lab's values (#362) and the evidence (#364).
  - #293: SPEC_S5 and its code (#363), and the evidence (#365).
- **The records:**
  - `docs/REVIEW_2026-09-23_release.md` covers #324–#335.
  - `docs/REVIEW_remote_sar_for_every_join.md` covers #336–#345.
  - The walks are in `reports/2026-09-23_release-walk/` and `reports/2026-09-24_d2b-lab-walk/`.

## Open PRs

None once this update's PR merges (`gh pr list --state open`, 2026-09-25).

## Work that was IN FLIGHT in background forks when the session ended

None. No fork or background agent carries work past this session.

- **The worktrees:** only the main checkout. `~/gitRepos/gsd-grafana` was removed by OB1's cleanup on 2026-09-25,
  with its three uncommitted PNGs; see the update above.
- **The local branches**, besides `main` and the branch this update was written on:
  - `feat/285-credential-lifecycle`: its head `078ebfd` is an ancestor of #325's head, merged as `048e928`.
  - `skill/review-models-fable`: its PR #137 closed unmerged.
- **The cleanup** (the orchestrator's summary): 42 scratch worktrees removed; 63 merged local branches and
  `integration/2026-09-23` deleted; and merged remote branches deleted in three sweeps: 23 of this session's PRs, 63
  older ones, and 3 more. Measured now: 4 local branches and 6 remote heads.
- **What became of the 2026-09-20 forks:**
  - #229 shipped as #233 and is closed.
  - #230's S1 shipped as #235, and its S2, the Cluster Configurations tab, as #237. #230 itself is still open.
  - Their worktrees, `~/gitRepos/gsd-library` and `~/gitRepos/gsd-clusterconfig`, no longer exist.
- **The coordination rule still holds:** only one `local-development/release-crc.sh` at a time on CRC.
  `pgrep -f release-crc.sh` must be empty, and the Argo Application Synced/Healthy, before you deploy.

## Decisions taken this session (the operator's words, where they ruled)

This session is 2026-09-23 → 25. The full account is in its session log.

**Login capture.** The audit log is the default source: *"we are moving away from oauth debug option to using
audit-logs"*. That superseded #318's "do not flip". #321 removes the Debug path; it is open, not built.

**SPEC_S4c's gate** (#325): the operator chose Codex's rule, gating every bound failure per account. That reverses
R2-1.

**The Namespace audit.** #261's part 4 was decided with no code: sideways scrolling inside the tables stays.

**The cluster-admin tier** (#322): `visibility.clusterAdminSar` asks `update clusterrolebindings`. It is decided and
not built.

**Remote cluster access** (`docs/DESIGN_remote_cluster_access.md`):

- **D2:** `remote-sar` + `same-as-host` is the standard.
- **D3:** readers are matched by OpenShift username: *"this is all OpenShift and everyone appears as a user … just
  focus on the users that you see in OpenShift users."*
- **D6:** no `inherit` stopgap.
- **D7:** joining or rejoining with a password is a cluster-admin action, checked on the host.
- **The mandate:** *"this is what I want: remote-sar for whatever method the cluster was joined"*. It is built
  (#342).
- **D5** was put in scope by the operator's go-ahead.
- `group-sync-operator-helm` installs the auditor objects on every remote.

**#341, the whole-UI redesign, is deferred:** *"I want this app to be functioning first"*.

**Reviews:**

- Codex must write its full fixes to a file: *"remember codex review must give us full code solution to any findings
  in file"*.
- The Codex invocation, as the operator gave it: `codex --model gpt-6-astra --config model_reasoning_effort="high"`.
- A spec is implemented from its written blocks alone. `b8e75b0`'s message records the operator's rule:
  implementation works from the finished, written document, not from memory. The tool is
  `local-development/apply-spec-blocks.py`.

**The lab:**

- *"pvc should not be removed"*. The kept PVCs' UIDs were unchanged through every Helm round and the hand-back.
- `shared-qa`'s expiry was on purpose: *"This was an intentional set to expires because we didnt use the long term
  token just to dematrate"*. Then: *"fix shared-qa with a long term token that does not expires and then rejoin it.
  Show this as well"*. That was done in Step 11.
- The in-cluster URL belongs to the controller only: *"`https://kubernetes.default.svc.cluster.local` - this is only
  connecting internally using the mounted ca into the pod for dashboard controller only."*

**Token cost.** Arm one background waiter per wait, and never poll. The operator's words were *"the ob3 is wasting my
token - we have a skill here that monitor a back ground and wake up. no polling is needed"* and *"I just killed a
process now. You wasted my token on that for over 230mins"*.

**Provenance** (#312, #353): *"Pls add the metadata that we desigh to suppress warnings on this kind of resources"*.
The chart labels its own RBAC with `rbac.ocp.io/config-source` (#350). The goal is hand-made grants on groups,
ServiceAccounts or users, and exclusion is the operator's decision, never inferred: *"The exclusion is not automatic
… We are going to decide who to exclude"*, *"Using that label. We just need capabilities"*, *"We will labeling the
role bindings or clusterrolebindings for users or groups or service account"*. The orchestrator's earlier
"Group-only on best practice" was retracted. The capability is #353, closed.

**The platform rule** (#353): *"silence all platform service account … those namespaces we excluded them in code and
also add additional ones via values.yaml"*; *"I wanna exclude by namespaces as designed"*, plus exactly OpenShift's
three per-project controller bindings, exact shape, in every namespace. SA `default` has no name rule. A User spelt
`system:serviceaccount:…` stays silent. No other method.

**ConfigMap onboarding** (#293): *"insecure is required in configmap"*, so `insecureSkipVerify` is allowed and the
residual trust is recorded. *"Whatever you do … do not reduce the current level of permissions that service account
has"*: every chart change is proven by a rendered RBAC diff, and removals must be 0.

**Seats:** `ob2` has two roles, named by the brief's first line (`Role: REVIEWER`, the default, or `Role:
IMPLEMENTER`).

**The host guard's other spellings** were examined after the walk and closed as not a gap: a Secret never names the
pod's token or CA file, so it is always read with its own credential.

**Still standing from 2026-09-20:**

- #229: the Reports and Reporting-status pages stay exactly as they are, and the library is its own tab.
- #230: clusters are labelled Secrets, on Argo CD's model. The label drives discovery, `stringData.name` is the
  cluster id, and the tab and GitOps write the same Secret.

## Mocks — status

The design index (`docs/design/README.md`) marks every mock **Implemented** since #351, which also removed its
duplicate rows; `local-development/tests/test_design_index.py` keeps each artefact listed once and every mock tagged.
`docs/design/data-requirements.md` is marked as the dated analysis of 2026-09-17; its marks are not maintained.

No mock is being made or is owed. #341, which would need new mocks, is deferred.

## Lab

- **The deployment:** CRC, with the Argo Application `group-sync-dashboard` in `openshift-gitops`. After #363
  merged, `release-crc.sh --argocd` deployed `09d5526`: Synced/Healthy, `09d5526cae` verified in-pod, and the PVCs
  identical to the baseline. `environments/crc.yaml` carries the reference `platformNamespaces` additions (#362).
- **Retired demo clusters:** #293's walk left `cm-demo-a` and `cm-demo-b` as retired rows with their history, by the
  keep-history rule.
- **The clusters:**
  - `shared-qa` is rejoined with a ServiceAccount token that has no expiry (Step 11).
  - `shared-rnd` is served from the Secret its lookup wrote.
  - The `mock` stanza is not served under Argo CD.
- **The kept PVCs:** `group-sync-dashboard-data` has UID `f065b7a4-535c-4ef1-868c-58f5afee4953`, and
  `group-sync-dashboard-report-artifacts` has UID `08c7d45c-a3eb-47be-8506-f24ea7a3e0e3`. Both were created on
  2026-09-19, and both were unchanged through every deploy of this session, the last of them `09d5526`
  (2026-09-25 06:48Z).
- **Credentials:** the kubeadmin password is at `~/.crc/machines/crc/kubeadmin-password`. The report service's token
  Secret is `group-sync-dashboard-report-shared-token`; query the service over `https://127.0.0.1:8443` from the
  report pod, with `Authorization: Bearer`.
- **The mock's port.** The walk's port-forward to the mock uses local port 16443. During the walk, a 5-day-old local
  `podman` container held that port (the walk record). The cleanup stopped a 5-day-old local podman mock (the
  orchestrator's summary).
- **Nothing published is committed.** No claude.ai URL is in the repository. This session's D2b evidence page and a
  gRPC diagram page were published privately.

## Waiters and processes

Wait on background work with one waiter per wait, and stay silent until it fires; never poll. Sweep stale processes
on the next start: `~/.claude/tools/sweep-stale.py` runs from the UserPromptSubmit hook.

## The resume prompt

> Read `docs/HANDOVER_2026-09-20.md` on main, then the memory index. #322 and #321 shipped on 2026-09-25; the order
> of the next work is the operator's. Candidates: #315 then #285 (SPEC_S4c, specified and not begun, at application
> 0.37.0 and chart 0.59.0), or #316 (Rejoin, gated by #322's tier). Also open: #210 (the Fable re-review), #341
> (deferred), #369 (parked) and #371 (the suite writes into the working tree). The implementers are OB1-lite and Codex
> Astra; a change is never reviewed by the seat that wrote it. Every PR gets the review seats, a CRC deploy and walk,
> evidence on the issue, and the session changelog. Never merge without CI green on the head's rollup, never deploy
> while another deploy runs, never reduce the dashboard ServiceAccount's permissions, remove only the worktrees you
> created, and use one background waiter per wait, with no polling.
