# Design mockups

Static HTML mockups of designs under review — open them in a browser. They are design
artifacts (not shipped pages); the implemented pages live under `local-development/gsd/static/`.

| Mockup | For |
|---|---|
| [`reporting-status-mock.html`](reporting-status-mock.html) | **Implemented** (#149 R5) — the reporting status / schedules / history UI at `#page=reporting` |
| [`report-form-mock.html`](report-form-mock.html) | **Implemented** (#149 R7) — the report input forms: one ParamSpec-driven shell, discovered lookups with type-ahead, subject scopes |
| [`landing-access-mock.html`](landing-access-mock.html) | **Implemented** (#158, merged; the walk under `reports/2026-09-18_home-158/`) — the self-scoped "Your access" landing page every user gets — what you can reach, where it came from, what changed — see issue #158 |
| [`overview-kpi-mock.html`](overview-kpi-mock.html) | **Implemented** (#157, merged) — the administrator Overview makeover — system status, access posture, trends, Grafana/Observe links — see issue #157 |
| [`tab-redesign-mock.html`](tab-redesign-mock.html) | **Implemented** (#153, merged; the trailing tabs brought to the bar) — the six tabs redesigned **feature-complete** against the contract — real rows, real caveats, real filters — see issue #153 |
| [`tab-feature-contract.md`](tab-feature-contract.md) | What a tab redesign may **not** remove — captured from the running dashboard and the render functions — see issue #153 |
| [`drilldown-mock.html`](drilldown-mock.html) | **Implemented** (#167, merged) — the drill-down experience — one pattern-matched lookup over users, groups **and** namespaces, then group → member → user → via-group, with the namespace as the third drillable entity — see issues #166 and #167 |
| [`cluster-overview-mock.html`](cluster-overview-mock.html) | **Implemented** (#157's relayout, merged) — the Cluster Overview relaid onto the shared design system — cluster tiles that shrink as the fleet grows, paged alerts, and every status carrying its consequence — see issue #157 |
| [`drilldown-mock.html`](drilldown-mock.html) | The drill-down experience — one pattern-matched lookup over users, groups **and** namespaces, then group → member → user → via-group, with the namespace as the third drillable entity — see issues #166 and #167 |
| [`cluster-overview-mock.html`](cluster-overview-mock.html) | The Cluster Overview relaid onto the shared design system — cluster tiles that shrink as the fleet grows, paged alerts, and every status carrying its consequence — see issue #157 |
| [`report-library-mock.html`](report-library-mock.html) | **Implemented** (#229, SPEC E1) — the Library tab, a new canvas beside the untouched Reports and Reporting-status pages: one section per configured report, built by JS from the three embedded lab payloads (the catalogue, `/api/status`, `/api/runs`), headed by the service's cadence in words ("Quarterly compliance"), each run with its formats, its failure reason and its expiry as `prune()` defines it, the run drawer at `#page=library&run=<id>`; drafted by Cursor Grok, six render-check findings fixed before agreement |
| [`cluster-configurations-mock.html`](cluster-configurations-mock.html) | **Agreed** (#230 S2, 2026-09-20) — the Cluster Configurations tab: every cluster with its source (values / a labelled Secret / in-cluster), the credential KIND only, the three TLS modes (the dashboard's trust store by default, `caData` per cluster, insecure — never both), D2's visibility and identity, the connection state; a malformed Secret as a finding; the Add-cluster form that writes the Secret with the "as GitOps would write it" YAML beside it. Built by JS from the lab's real `/api/clusters`, `crc.yaml` and namespace-Secret payloads; drafted by Cursor Grok, four render-check findings fixed (a syntax error, a chip 58 px past 375, YAML indentation, an empty card) |
| [`nsaudit-mock.html`](nsaudit-mock.html) | **Proposed** (2026-09-21, nothing ships from this) — the Namespace audit tab, the one major tab that never got a mock: the verdict as a tier partition with one marked cell per rollup entry, the cluster scope lifted out of the namespace table into its own card because its blast radius is all 106 namespaces, a worklist whose namespace names are links (they are plain text today), and the namespace page's 975-character cluster-wide sentence turned into a disclosure of 16 rows. The namespace index — 3,619 px of a 5,153 px page, of which 67 of 106 rows are the platform's own — gets four controls that compose: the platform filter (the product's own `is_platform_namespace`, applied to this index for the first time, hiding 67 and counting them), numbered pages of 25 over what is left, a fold that starts closed above 25 rows, and a free-form search of its own beside it; the bar's inherited **Find namespace** keeps exactly the scope the shell gives it, and every count on the page quotes its denominator Every number embedded from the lab (`/user-bindings`, `/namespaces`, six namespace pages, the self tier through the pod loopback); the four states the lab cannot show are drawn and labelled as shapes. Render-checked at 375/393/768/1280 with contrast measured from painted pixels — `reports/2026-09-21_nsaudit-mock/` |
| [`nsaudit-feature-capture.md`](nsaudit-feature-capture.md) | What the Namespace audit page holds today, measured off the running dashboard before anything was drawn — every KPI, column, filter, empty state, caveat, badge, refusal, export and drill path with the render function it comes from, plus the three states that exist in the code and not in the lab; the source for the contract's new `### Namespace audit` section |
| [`reports-page-mock.html`](reports-page-mock.html) | **Implemented** (#173) — the Reports page as used: the catalogue (bolder names, a colour gradient per report) and the per-report form a click opens immediately, in view, as a shareable position; design lineage #149 |
| [`data-requirements.md`](data-requirements.md) | What every mock renders, where each number comes from today, and the ten gaps that stand between the mocks and real pages — see issues #156, #167, #149, #158 |
| [`inventory-2026-09-17.md`](inventory-2026-09-17.md) | The dated inventory: every finished mock, its owning issue, its review rendering, the decisions it embodies, and what was retired |
| [`kyverno-research-2026-09-17.md`](kyverno-research-2026-09-17.md) | The Kyverno research record — ten findings, each cited to the v1.19.1 source or shipped CRDs, and the correction to #165's removal-date claim |
| [`kyverno-step0-discovery-2026-09-19.md`](kyverno-step0-discovery-2026-09-19.md) | Step 0, first pass: the served report API, the result shape, the namespace-qualified `policy` string, and the finding-10 probe — VAP generation is per-policy opt-in and the VAP-admitted object reported under neither source within 150 s |
| [`kyverno-discovery-2026-09-20.md`](kyverno-discovery-2026-09-20.md) | Step 0, re-measured: the report groups and CRD versions, the controllers' flags, the `source` field that tells the CEL family from the legacy one, `rule` absent for CEL, finding 10's answer (no VAP because the opt-in is off — `status: skip generating ValidatingAdmissionPolicy: not enabled.`; once generated, the policy reports only under the VAP), the three breakers — #170 step 0 |
