# SPEC_D2b on the lab — #342 walked, 2026-09-24

Walked on the CRC lab (OpenShift 4.22.7) against #342, head `8e50ff8` (chart 0.53.0, app 0.32.0): two Helm rounds with
the dashboard Application's automated sync paused, then `local-development/release-crc.sh --argocd` and the recorded
sync policy restored. Every step's command is in `docs/specs/SPEC_D2b_remote_sar_for_every_join.md` §5 as #344
corrected it; the scripts that ran them and their raw output are in `walk/`, and each row below quotes that output.

## Outcome

| What | Result |
|---|---|
| SPEC_D2b §5, Steps 0–11 | every step passes, with the notes below |
| the D5 line, corrected check (one fresh page per reader and cluster) | **16 / 16** with `shared-qa` expired on purpose; **4 / 4** after Step 11 |
| `shared-qa` rejoined with a long-lived token (Step 11) | the token has no `exp`; resolved with no restart; polled every minute since |
| e2e walk, run after the rejoin | **84 / 84** steps, **24 / 24** extra, **11 / 11** PDFs rendered, **11 / 11** reports pass integrity |
| the kept PVCs | the same UIDs, volumes and creation times before and after every Helm round and the hand-back |

## What the walk found

- **Lab tooling, fixed in #343:** `release-crc.sh`'s Helm handover was refused once the chart version moved — the kept
  PVCs' labels were owned by `argocd-controller`, and Helm 4 applies server-side. It now passes `--force-conflicts`.
- **The spec's lab steps, fixed in #344:** Step 6's page check could pass on the previous cluster's text; `shared-qa`'s
  token is expired on purpose, so Steps 6 and 10 expect `self` there until Step 11; and Step 7's in-cluster URL row
  asked for a URL the parser refuses by design. A substitute URL tried for that row is retracted: every in-cluster
  name is the dashboard controller's own endpoint, never a remote's (the operator).
- **Open for the operator:** the host guard refuses only the exact string `https://kubernetes.default.svc`, so other
  spellings of the in-cluster endpoint pass it.
- **Not measured:** Step 9's check that the mock is not asked was stopped before it ran, and could not have been
  trusted: a stale local `podman` container held the port its port-forward uses.

## The steps

| Step | What it checks | Expected | Result |
|---|---|---|---|
| 0 | Sessions and names | token session (`oc whoami -t`), branch at the repo root and pushed | `kubeadmin`, token session yes; root on `feat/338-remote-sar-every-join`, pushed — **PASS** |
| 1 | Record the Application's sync policy, pause automated sync | policy saved; `automated` removed | saved `{"automated":{"prune":true,"selfHeal":true},"retry":{…"limit":3},"syncOptions":["CreateNamespace=true","RespectIgnoreDifferences=true","ServerSideApply=true"]}`; after the patch `automated` is gone — **PASS** |
| 2 | The personas on both servers | kubeadmin both, jane.smith host-only, developer remote-only, test-user-002 neither | CRC `list clusterrolebindings`: kubeadmin yes, jane.smith yes, developer no, test-user-002 no; mock `/_mock/sar-probe`: kubeadmin allowed (`cluster-admin-crb`), jane.smith denied, developer allowed (`report-auditors-crb`), test-user-002 denied — **PASS** |
| 3 | Helm round 1: every join path at the default | `mock` start-up line `remote-sar, identity same-as-host`; `shared-qa` and `shared-rnd` resolved `visibility=remote-sar identity=same-as-host enabled=true` | First run **FAILED** before install: `release-crc.sh`'s Helm handover was refused by a server-side-apply conflict on the kept PVCs' labels (owned by `argocd-controller`) — a pre-existing tooling defect, fixed in #343 (`--force-conflicts`; reviewed, CI 8/8, merged `5b7d346`). Rerun on `8e50ff8` (#342 with main): `running : 8e50ff8eec — verified in-pod`; `mock: per-cluster visibility policy remote-sar, identity same-as-host`; `cluster=shared-qa … visibility=remote-sar identity=same-as-host enabled=true`; `cluster=shared-rnd source=secret:gsd-cluster-shared-rnd … visibility=remote-sar identity=same-as-host enabled=true` (its Secret still states `self-only`/`none`: the stanza's pair is served) — **PASS**. PVCs: identical UIDs, volumes and creation times before and after (`pvc-baseline.txt`, `pvc-after-step3.txt`). |
| 4 | Ownership, then the lookup's own write | annotation removed → `self-only`/`none` and the `shadows-values-entry` finding; restored → `remote-sar`/`same-as-host`, finding clears; Secret deleted → the lookup writes it again with `visibility=remote-sar`, `identity=same-as-host` | cycle 2: `visibility=self-only identity=none`, findings `["shadows-values-entry"]`; cycle 3: `visibility=remote-sar identity=same-as-host`, findings `[]`; deleted, rewritten after 305 s: `visibility=remote-sar`, `identity=same-as-host`; cycle 5 resolves `remote-sar`/`same-as-host` — **PASS** |
| 5 | A Secret created at runtime, and the identity exception | `mock-sar` written `remote-sar`/`same-as-host`, `mock-none` `self-only`/`none`; within one discovery, no restart, `mock-sar` resolved `remote-sar`; developer `all` on `mock-sar`; developer `/api/clusters/mock-none/groups` → 403 | POST answered `{"secret":"gsd-cluster-mock-sar",…,"discovery":"requested"}` and the same for `mock-none`; keys `mock-sar visibility=remote-sar identity=same-as-host`, `mock-none visibility=self-only identity=none`; cycle 9 `cluster=mock-sar … tls=caData visibility=remote-sar identity=same-as-host enabled=true`, cycle 10 `cluster=mock-none … visibility=self-only identity=none`; developer whoami `mock-sar remote-sar same-as-host all`, `mock-none self-only none self`; `mock-none/groups` → `403` — **PASS** |
| 6 (first run) | The personas on every cluster, and D5's line | kubeadmin `all` on every remote; jane.smith `all` on `shared-rnd`/`shared-qa`, `self` on `mock`/`mock-sar`; developer the reverse; test-user-002 `self` everywhere; `groups`/`home` 200 with own rows where `self`, `bindings/findings` 403 | Every row as expected **except `shared-qa` for kubeadmin and jane.smith: `self`, not `all`**. Cause, measured: `shared-qa`'s Secret holds a 10-minute ServiceAccount token (`sub system:serviceaccount:group-sync-operator:shared-qa-poller`, iat 2026-09-23T01:47:12Z, **exp 2026-09-23T01:57:12Z**), rejected since this pod's first poll (`02:49:51 … 401`); `gsd.kube shared-qa: visibility tier for 'kubeadmin' is indeterminate (auth_failed: 401 …) — failing closed to the self view`. This token is **intentionally short-lived** (the operator: set to expire, to demonstrate the path rather than use a long-term token), so `shared-qa` is the lab's expired-credential case: the fail-closed path and the 30 s hold behaved as specified (jane.smith was answered `self` without a second call). **Found by the walk — a defect in the spec's Step 6 script:** its page check navigates by hash on one page per persona and passed kubeadmin and jane.smith on `shared-qa` on the previous cluster's text; one fresh page per cluster shows `This cluster's own RBAC shows your own rows, or could not be asked.` for both (`step6-recheck.out`). D5: 20/20 lines as expected once the stale match is removed, `shared-qa` narrowed for all four. `shared-qa`'s wide view is shown in Step 7's working-token rows; after Step 7 it is restored to its intentional expired token, as the spec's restore says. |
| 7 | Rotation without a restart (token by the tab's route; CA and URL by editing the Secret) | bad token → `self` + `auth_failed`; new token → `all` at once; wrong CA → `self` + `unreachable` (verification); own CA → `all` at once; dead URL → `self` + `unreachable`; in-cluster URL → `all` at once | bad token: cycle 11, `self`, `auth_failed: 401`; new token (a `shared-qa-poller` token Secret): cycle 12, **`all`**; the mock's CA: cycle 13 `tls=caData`, `self`, `unreachable: … CERTIFICATE_VERIFY_FAILED`; `kube-root-ca.crt`: cycle 14, **`all`**; `:6444`: cycle 15, `self`, `unreachable: … Connection refused` — **PASS (5/5)**. In-cluster URL `https://kubernetes.default.svc`: **refused by design** — `secret-refused … outcome=host-cluster-not-from-secret` (cycle 16: `removed=shared-qa`, "its Secret is gone; the poll thread stops (history kept)", absent from `/api/whoami`): the parser refuses a Secret whose server is exactly `IN_CLUSTER_SERVER`, the host's in-cluster URL (`local-development/gsd/clusterconfig/parser.py`). **Found by the walk — a defect in the spec's Step 7 table**, not in the code: that row expects a URL the reader is designed to refuse. |
| 7 (cont.) | The URL row by another name, then the restore | a working in-cluster URL → `all` at once; restore the saved Secret; delete the scratch token Secret | ~~`https://kubernetes.default.svc.cluster.local`: cycle 20, `all`~~ — **RETRACTED** (the operator): that is the in-cluster endpoint only the dashboard controller uses, with the CA mounted into its pod; a remote's Secret must not point at it, so the row tested nothing about a remote. It shows instead that the parser refuses only the exact string `https://kubernetes.default.svc` — **finding: other spellings of the in-cluster endpoint pass the host guard**, for the operator to decide. A URL change taking effect with no restart is shown by the `:6444` row; restored exactly as the spec writes it: cycle 21 `tls=trusted-bundle`, `self`, `auth_failed: 401` (the token's exp 2026-09-23T01:57:12Z, intentional), server `https://api.crc.testing:6443`; `shared-qa-poller-token` deleted (`NotFound` after) — **PASS**. Step 7 in all: token 2/2, CA 2/2, URL 1/1 (the refused dead URL) used at once with no restart; the spec's in-cluster row is invalid. |
| 8 | Failure paths and the hold: joins without `create subjectaccessreviews` and without `list groups`, three personas for two minutes | per cluster one WARNING per persona in the first burst, then at most one per 30 s — at most 3 + 4 per cluster, 14 for the pair — each naming its cluster and `forbidden`; `outcome="forbidden"` rising by the same count; `GroupSyncDashboardVisibilityChecksFailing` pending | both resolved `remote-sar`/`same-as-host` (cycles 22, 23); warnings **4 per cluster, 8 for the pair** (≤ 14), at 04:09:10, 04:09:41, 04:10:11, 04:10:41 — one per 30 s per cluster, the first failure arming the hold before the other personas arrived; `d2b-no-sar`: `forbidden: 403 … lacks create subjectaccessreviews (the system:auth-delegator grant)`, `d2b-no-groups`: `forbidden`; metric `gsd_visibility_tier_checks_total{outcome="forbidden",threshold="admin"}` **0 → 8**; Thanos Ruler: the alert **`pending`** since 08:08:57Z (`for: 15m`); cleanup: both DELETEs 200, namespace, bindings and roles deleted — **PASS** |
| 9 | Lifecycle: a retired and a disabled cluster are never asked | `mock-sar`/`mock-none` deleted; Helm round 2 with `shared-rnd` and `mock` `enabled: false`; neither served; the mock's request log unchanged over a minute of traffic | DELETEs done; Helm revision 3 deployed (chart 0.53.0, image `0.32.0-8e50ff8eec`); `/api/whoami` clusters `['dashboard', 'mock-privateca', 'mock-selfsigned', 'mock-trusted', 'shared-qa']` — `shared-rnd`, `mock`, `mock-sar`, `mock-none` absent — **PASS**; PVCs identical to the baseline. The mock-log check was stopped by the operator before it ran; it also could not have been trusted: a 5-day-old local `podman run -p 16443:6443 mock-openshift:test` holds local port 16443, which the spec's port-forward uses (Step 2's mock answers may likewise have come from that local container, which loads the same fixture). |
| 10 | Hand back to Argo CD, restore the sync policy, the Definition of Done | Synced/Healthy, the pod running the head; `shared-rnd` and `shared-qa` both `remote-sar`/`same-as-host`; kubeadmin `all` on both; developer and test-user-002 `self` on both | `release-crc.sh --argocd`: `Synced/Healthy`, `running : 8e50ff8eec — verified in-pod`; syncPolicy already the recorded one; PVCs identical to the baseline (a Helm → Argo handback with no conflict: Argo CD forces server-side-apply conflicts, as #343's review decision read in its source); `shared-rnd` (its Secret, rewritten by the lookup in Step 4) and `shared-qa` (the default) both `visibility=remote-sar identity=same-as-host enabled=true`; kubeadmin `shared-rnd` **all**, `shared-qa` **self** — the intentionally expired token, not a defect; developer and test-user-002 `self` on both — **PASS** (with `shared-qa`'s wide view shown in Step 7's working-token rows) |
| 6 (corrected check, #344) | The personas on every cluster with `shared-qa` expired on purpose; one fresh page per reader and cluster | `shared-qa` narrowed for every reader; the rest as Step 6 | 16 / 16 page checks **ok** (4 readers × `dashboard`, `shared-rnd`, `shared-qa`, `mock-sar` re-created through the tab): `shared-qa` "This cluster's own RBAC shows your own rows, or could not be asked." for all four; `shared-qa`'s token `exp 2026-09-23T01:57:12Z`; its card: `auth_failed · 401 Unauthorized — token invalid or expired`. `mock` is not served under Argo CD, so it was not re-run — **PASS** |
| 11 (#344) | Rejoin `shared-qa` with a long-lived token through the tab's credential route | the token has no `exp`; `shared-qa` resolved again with no restart; the poller polls it; kubeadmin and jane.smith `all`, developer and test-user-002 `self` | `shared-qa-poller-token` (a ServiceAccount token Secret): **`exp NONE (no expiry)`**; `PUT …/shared-qa/credential` → `"discovery":"requested"`; cycle 6 `cluster=shared-qa visibility=remote-sar identity=same-as-host enabled=true` (09:08:03); `polled shared-qa: 3 CRs, 62 groups` at 09:08:46, 09:09:46, 09:10:46, no `auth_failed` since (the one warning, 09:07:53, is the expired check before it); `/api/whoami`: kubeadmin and jane.smith `shared-qa=all`, developer and test-user-002 `self`; page: kubeadmin and jane.smith "…gives you the full view.", developer and test-user-002 own rows (4 / 4); its card: `ok · reachable` — **PASS** |
| e2e (again) | The full e2e walk after the rejoin | every step, the second pass, every report | **84 / 84** steps, **24 / 24** extra, **11 / 11** PDFs rendered, **11 / 11** reports pass integrity (the first run, before the rejoin, gave the same) — **PASS** |

## Files

- `results.json`, `integrity.jsonl`, `env.json` and `e2e-walk.html`: the e2e walk after the rejoin; `artefacts/`, the
  eleven reports it generated (HTML and JSON; the PDFs are not committed, for size).
- `screenshots/`: the D5 line for kubeadmin and developer on `shared-rnd` and kubeadmin on `shared-qa`, and `shared-qa`
  expired and rejoined — its card and kubeadmin's line.
- `walk/`: each step's script and its output. `lib.sh` holds Step 0's helpers; the scripts read the password from the
  environment and never print a token.
