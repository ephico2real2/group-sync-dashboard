# Clusters as labelled Secrets on CRC — S1's validation (#230)

`validate.sh` beside this file, run against the deployed head through `oc` and the pod's loopback as
`kubeadmin`. No page: S1 has no page; the tab is S2. Every line below is the script's own output.

(filled in after the deploy)
