# 2026-09-19 — the generated Secrets minted on the cluster by a hook, never in the manifest (release grafana, CRC)
helm history grafana -n group-sync-dashboard (tail):
  9   superseded  Upgrade complete                       <- the lookup-era chart: the Secrets in the manifest
  10  failed      pre-upgrade hooks failed               <- the mint hook's SA was an ordinary resource: "serviceaccount not found" to its deadline
  11  failed      post-upgrade hooks failed              <- the pre-upgrade mint kept both; Helm then DELETED both (removed from the manifest); the gate could not see the datasource
  12  failed      post-upgrade hooks failed              <- pre+post mint: both re-created; the datasource sat in the operator's backoff (NoMatchingInstance since 23:09) past the gate
  13  deployed    Upgrade complete                       <- Helm still diffed against rev 9: deleted + re-minted (rv 591699/591728 -> 596822/596848); the gate's nudge; done
next upgrade from rev 13: resourceVersions 596822 596848 before and after -> KEPT; datasource synchronised 23:36:15Z; done

# the operator behaviour the nudge answers (grafana-operator v5.24.0 source):
#   controllers/datasource_controller.go:116-122  no ready instance -> return ctrl.Result{}, error   (controller-runtime exponential backoff)
#   datasource_controller.go:365-375              For(GrafanaDatasource), Watches(Secret), Watches(ConfigMap)  -> no watch on Grafana
# measured: NoMatchingInstance=True held from 23:09:10Z with the instance complete/success; a metadata-only annotate on the CR did nothing;
#           a spec change (generation bump) reconciled at once -> DatasourceSynchronized=True; the gate annotates the service-ca ConfigMap instead.

# after: the Grafana door through the OpenShift login on the minted cookie/admin: /api/user 200 kubeadmin isGrafanaAdmin=True; 24 panels
